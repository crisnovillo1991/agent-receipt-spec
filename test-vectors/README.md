# Test vectors

Deterministic vectors generated by `../tools/generate_vectors.py` with a
**public** test key (see `KEY.txt` — never reuse it). `expected.json` records
the entry hash and expected outcome of every check. Raw settle responses for
re-derivation testing live under `disclosures/`.

## v0.1 (byte-identical to the v0.1 release)

| Vector | standalone | chain vs 02 | teaches |
|---|---|---|---|
| `valid/01-free-call-seq0` | pass | — | minimal receipt, `payment: null` |
| `valid/02-paid-call-seq0` | pass | — | x402 payment binding |
| `valid/03-paid-call-seq1-chained` | pass | pass | hash chaining |
| `invalid/10-tampered-amount` | **fail** | — | signature covers everything |
| `invalid/11-valid-sig-wrong-prev` | pass | **fail** | signatures necessary, not sufficient |
| `invalid/12-float-amount` | **fail** | — | §4 number rule |

## v0.2 (settlement lifecycle — the failure cases)

| Vector | standalone | pair / chain | re-derivation | teaches |
|---|---|---|---|---|
| `valid/04-v02-receipt-settled-sync` | pass | — | pass (disclosure 04) | settled at issuance, response digested |
| `valid/05-v02-receipt-pending` | pass | — | — | pending ≠ settled: not dispute-grade for finality (§8.3) |
| `valid/06-v02-attachment-settled` | pass | pass vs 05 | pass (disclosure 06) | late settlement as new chain entry |
| `valid/07-v02-receipt-pending-2814` | pass | — | — | the receipt awaiting the #2814 case |
| `valid/08-v02-attachment-failed-2814` | pass | pass vs 07 | pass (disclosure 08) | **false assurance**: "processing" derives `failed`; verbatim digest carries the claim |
| `invalid/13-v02-rederivation-mismatch` | pass | — | **fail** (disclosure 13) | tampered tx_hash caught on disclosure (§8.4) |
| `invalid/14-v02-attachment-dangling` | pass | **fail** vs 07 | — | attaches_to must match the receipt (§8.2) |
| `invalid/15-v02-missing-settlement-status` | **fail** | — | — | payment requires settlement_status (§4.3) |
